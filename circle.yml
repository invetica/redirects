machine:
  java:
    version: oraclejdk8
  environment:
    _JAVA_OPTIONS: "-Xms512m -Xmx2048m"

test:
  override:
    - lein trampoline test
    # Bring back Eastwood!
    #
    # https://github.com/invetica/redirects/issues/2
    - lein whitespace-linter
